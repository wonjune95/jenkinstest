# playbook.yml
---
- name: Build and Push Docker image on Master
  hosts: master
  tasks:
    - name: Log in to Docker Hub
      command: docker login -u wonjune95 -p dnjswns95!!
      no_log: true

    - name: Build Docker image
      command: docker build -t wonjune95/keduit:puple .
      args:
        chdir: /root/portal

    - name: Push Docker image to Docker Hub
      command: docker push wonjune95/keduit:puple

    - name: Deploy
      command: sudo kubectl create deploy web-puple --replicas=3 --port=80 --image=wonjune95/keduitlab:puple

    - name: Expose
      command: sudo kubectl expose deploy web-puple --type=LoadBalancer --port=80 --target-port=80 --name=web-puple-svc
